document.addEventListener("DOMContentLoaded", function(){
    // 获取邮箱输入框和remember Me复选框
    const emailInput = document.getElementById("email");
    const remeberCheckbox = document.getElementById("remember");

    //从Cookies读取保存的邮箱
    const savedEmail =
})